name: UPI_Fraud_Monitoring
version: 1.0.0
config-version: 2
profile: UPI_Fraud_Monitoring
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  UPI_Fraud_Monitoring:
    +materialized: view